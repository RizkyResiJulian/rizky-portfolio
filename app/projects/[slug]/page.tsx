import Link from 'next/link';import {notFound} from 'next/navigation';import {projects} from '@/data';
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)return notFound();return <><div className="container case-hero"><Link className="back" href="/#work">← BACK TO WORK</Link><div className="section-kicker" style={{marginTop:40}}>{p.number} / CASE STUDY</div><h1 className="section-title">{p.title}</h1><p className="lead" style={{maxWidth:800}}>{p.desc}</p><div className="case-meta">{p.tags.map(t=><span className="tag" key={t}>{t}</span>)}</div><div className="case-cover"><img src={p.image} alt="Project placeholder"/></div><div className="case-grid"><article className="case-content"><h2>Overview</h2><p>This page is prepared as an editable case-study template. Replace the placeholder content with the actual problem, process, technical decisions, screenshots, results and your personal contribution.</p><h2>Problem</h2><p>Describe the operational or user problem that motivated the project and what made the existing process inefficient.</p><h2>Solution</h2><p>Explain how the system solves the problem, including architecture, key features, data flow and implementation choices.</p><h2>Result</h2><p>Add measurable outcomes here: time saved, reporting accuracy, number of users, automation improvements or other evidence.</p><h2>Gallery</h2><p>Place additional screenshots and diagrams in the project assets folder when you are ready.</p></article><aside className="side-box"><strong>PROJECT INFO</strong><p className="muted">Role<br/><b style={{color:'#fff'}}>Developer / IT</b></p><p className="muted">Stack<br/><b style={{color:'#fff'}}>{p.tags.join(' · ')}</b></p><a className="view" href="https://github.com/rizkyresijulian" target="_blank">GITHUB →</a></aside></div></div></>}
